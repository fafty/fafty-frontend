{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/fafty-creator",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/fafty-creator",
        "outputPath": "dist/apps/fafty-creator"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/fafty-creator"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "fafty-creator:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "fafty-creator:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "fafty-creator:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "fafty-creator:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/fafty-creator"],
      "options": {
        "jestConfig": "apps/fafty-creator/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/fafty-creator/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
